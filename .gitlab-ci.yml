before_script:
  - echo "before script"
build_job:
  only:
    - web
  script:
    - cd .\\scripts
    - "powershell -file .\\build.ps1"
  when: manual
clean_job:
  only:
    - web
  script: "powershell -file .\\scripts\\clean.ps1"
  when: manual