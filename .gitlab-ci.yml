before_script:
  - echo "before script"

stages:
  - build
  - test
  - deploy

build_job:
  stage: build
  only:
    - web
  script:
    - cd .\\scripts
    - "powershell -file .\\build.ps1"
  when: manual
clean_job:
  only:
    - web
  script: "powershell -file .\\scripts\\clean.ps1"
  when: manual

test_build:
  stage: test
  only:
    - web
  script:
    - cd .\\scripts
    - "powershell -file \\test.ps1"
  when: manual